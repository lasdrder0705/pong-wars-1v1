Original prompt: 我有一个域名，可以把这个游戏部署到这个域名上吗？这个域名原本就已经有服务器连接上了，我想把这个游戏部署到服务器上，然后可以通过域名访问这个游戏

# 部署进度

- 2026-08-31：开始检查公网域名部署兼容性。
- 已确认运行架构为 Node.js 静态文件服务与同源 `/ws` WebSocket 中继。
- 已确认当前版本需要补充 HTTPS 域名 Origin 白名单，并避免公网页面自动切换到服务器内网 `ws://` 地址。
- 已增加 `GAME_PUBLIC_ORIGINS` 精确白名单、`GAME_BIND_HOST` 监听地址和 `/healthz` 健康检查。
- 已让 HTTPS 公网页面自动使用同源 `wss://域名/ws`，同时保留 Android APK 手动连接局域网的能力。
- 新增公网 Origin 与同源 WSS 回归用例；完整测试当前为 22/22 通过。

# 待办

- 添加 Nginx 与 systemd 部署模板、上线文档和健康检查。
- 获得实际域名和服务器接入方式后，完成远端部署、证书和双端联机验收。
